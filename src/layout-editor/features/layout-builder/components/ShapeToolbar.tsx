// ShapeToolbar is reserved for future use (shape sub-selection on canvas).
// The left sidebar exposes shape variants directly, so this component renders nothing.
export function ShapeToolbar() {
  return null;
}
