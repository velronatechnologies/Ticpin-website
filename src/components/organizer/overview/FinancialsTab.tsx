'use client'; import React from 'react';
import { IndianRupee } from 'lucide-react';
import { BookingData } from './types'; interface FinancialsTabProps { bookings: BookingData[]; selectedBookings: string[]; setSelectedBookings: React.Dispatch<React.SetStateAction<string[]>>; triggeringPayout: boolean; payoutMessage: string; handleTriggerPayout: () => Promise<void>;
} export default function FinancialsTab({ bookings, selectedBookings, setSelectedBookings, triggeringPayout, payoutMessage, handleTriggerPayout
}: FinancialsTabProps) { return ( <div className="space-y-8 animate-fadeIn"> <div> <h1 className="text-3xl font-semibold text-black">Payout Center</h1> <p className="text-zinc-500 mt-1">Review eligible bookings, calculate commissions, and trigger payout approvals.</p> </div> {/* Payout calculation snapshot */} <div className="bg-white rounded-2xl p-6 border border-[#aeaeae] space-y-4"> <h3 className="text-lg font-bold text-black">Trigger Payout Requests</h3> <p className="text-sm text-zinc-500"> Choose the completed ticket orders below. Our system automatically processes bank transfers after deducting a <strong>5% platform fee</strong>. </p> <div className="grid grid-cols-2 md:grid-cols-3 gap-6 pt-2"> <div className="p-4 bg-zinc-50 rounded-xl border border-[#AEAEAE]"> <p className="text-zinc-400 text-xs font-bold uppercase tracking-wider">Eligible Orders</p> <p className="text-2xl font-bold text-black mt-1"> {bookings.filter(b => b.payout_status === 'none').length} </p> </div> <div className="p-4 bg-amber-50 rounded-xl border border-[#AEAEAE]"> <p className="text-amber-800 text-xs font-bold uppercase tracking-wider">Pending Release</p> <p className="text-2xl font-bold text-amber-700 mt-1"> {bookings.filter(b => b.payout_status === 'pending_approval').length} </p> </div> <div className="p-4 bg-emerald-50 rounded-xl border border-[#AEAEAE] col-span-2 md:col-span-1"> <p className="text-emerald-800 text-xs font-bold uppercase tracking-wider">Settled Payouts</p> <p className="text-2xl font-bold text-emerald-700 mt-1"> {bookings.filter(b => b.payout_status === 'approved').length} </p> </div> </div> {selectedBookings.length > 0 && ( <div className="p-4 bg-purple-50 border border-[#AEAEAE] rounded-xl flex items-center justify-between"> <div> <span className="text-sm font-semibold text-purple-900"> {selectedBookings.length} bookings selected </span> <p className="text-xs text-purple-700 mt-0.5"> Net Payout (Estimated 95%): ₹ {( bookings .filter(b => selectedBookings.includes(b.id)) .reduce((sum, b) => sum + b.grand_total, 0) * 0.95 ).toLocaleString()} </p> </div> <button onClick={handleTriggerPayout} disabled={triggeringPayout} className="px-5 py-2 bg-[#5331EA] hover:bg-[#4223ca] text-white rounded-lg text-sm font-semibold transition-colors disabled:opacity-50" > {triggeringPayout ? 'Triggering...' : 'Request Payout Now'} </button> </div> )} {payoutMessage && ( <p className="text-sm font-semibold bg-emerald-50 text-emerald-700 p-3 rounded-lg border border-[#AEAEAE]"> {payoutMessage} </p> )} </div> {/* Payouts list table */} <div className="bg-white rounded-2xl border border-[#aeaeae] overflow-hidden "> <div className="p-6 border-b border-[#AEAEAE] bg-zinc-50/50 flex justify-between items-center"> <span className="font-bold text-zinc-700">Eligible Booking Transactions</span> {bookings.filter(b => b.payout_status === 'none').length > 0 && ( <button onClick={() => { const allNone = bookings.filter(b => b.payout_status === 'none').map(b => b.id); setSelectedBookings(prev => prev.length === allNone.length ? [] : allNone); }} className="text-xs font-semibold text-purple-700 hover:text-purple-900" > Toggle All </button> )} </div> {bookings.length === 0 ? ( <p className="p-8 text-center text-zinc-400 font-medium">No booking transactions found.</p> ) : ( <div className="overflow-x-auto"> <table className="w-full text-left border-collapse"> <thead> <tr className="bg-zinc-50 border-b border-[#AEAEAE] text-zinc-400 font-semibold text-xs uppercase tracking-wider"> <th className="px-6 py-4 w-12">Select</th> <th className="px-6 py-4">Booking ID</th> <th className="px-6 py-4">Amount</th> <th className="px-6 py-4 text-center">Net (95%)</th> <th className="px-6 py-4 text-center">Payout status</th> <th className="px-6 py-4">Date</th> </tr> </thead> <tbody className="divide-y divide-[#AEAEAE] text-[14px]"> {bookings.map((b) => ( <tr key={b.id} className="hover:bg-zinc-50/50 transition-colors"> <td className="px-6 py-4.5"> {b.payout_status === 'none' ? ( <input type="checkbox" checked={selectedBookings.includes(b.id)} onChange={() => { setSelectedBookings(prev => prev.includes(b.id) ? prev.filter(x => x !== b.id) : [...prev, b.id] ); }} className="w-4 h-4 rounded text-[#5331EA] border-[#AEAEAE] focus:ring-[#5331EA]" /> ) : ( <span className="text-zinc-300">-</span> )} </td> <td className="px-6 py-4.5 font-mono font-bold text-zinc-700"> #{b.booking_id} </td> <td className="px-6 py-4.5 font-bold text-black"> ₹{b.grand_total.toLocaleString()} </td> <td className="px-6 py-4.5 text-center font-semibold text-emerald-600"> ₹{(b.grand_total * 0.95).toLocaleString()} </td> <td className="px-6 py-4.5 text-center"> <span className={`inline-block px-2.5 py-0.5 rounded-full text-xs font-bold capitalize ${ b.payout_status === 'approved' ? 'bg-emerald-100 text-emerald-800' : b.payout_status === 'pending_approval' ? 'bg-amber-100 text-amber-800' : 'bg-zinc-100 text-zinc-700' }`}> {b.payout_status === 'none' ? 'eligible' : b.payout_status.replace('_', ' ')} </span> </td> <td className="px-6 py-4.5 text-zinc-400"> {new Date(b.booked_at).toLocaleDateString()} </td> </tr> ))} </tbody> </table> </div> )} </div> </div> );
}
