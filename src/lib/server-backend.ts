// Server-rendered pages should use the same internal backend proxy path as the
// browser so hosted SSR requests follow the same routing and headers.
export const SERVER_BACKEND_API_BASE = '/backend/api';
